# TODO

## [2026-08-26 — актуально] BIG: Выбор AI-провайдера + фолбэк для Edge Functions

### Контекст (не сессионный — читать перед стартом)

- Обе edge functions (`generate-lesson`, `lookup-word`) сейчас жёстко на **OpenAI (`gpt-4o`)**. Единственная точка отказа.
- Прецедент: **Kino-app 2026-08-16** — `movie-recommendation` полтора месяца молча лежала после того как DeepSeek снял `deepseek-chat` 24 июля. Правило в `AI_OS/MEMORY/tasks/cross-repo-todo.md`: «один платный провайдер = точка отказа».
- Готовое решение того же класса — **Kino-app `supabase/functions/movie-recommendation/index.ts:110-217`**: чистый паттерн «primary → fallback», можно скопировать структуру целиком.
- Уроки не пропускать: этот проект **уже нарушал** правило моратория на API-мерж (см. `AI_OS/MEMORY/lessons/lessons.md` [2026-08-22]). **Мерж только вручную кнопкой веб-интерфейса**, деплой edge functions — через Supabase MCP (`deploy_edge_function`).

### Скоуп сессии — только это, без расширения

Обе функции получают выбор провайдера через переменные окружения Supabase Secrets + retry/fallback на второй провайдер при отказе первого. Никаких новых фич, никакого UI-выбора провайдера для пользователя (пользователь сам не должен выбирать — это internal reliability).

### Решения, которые владелица должна утвердить ДО начала кода

- [ ] **Основной провайдер** — оставить OpenAI `gpt-4o` (текущий) или сменить?
- [ ] **Fallback провайдер** — какой из трёх (в порядке предложения):
  1. **DeepSeek** (рекомендация): OpenAI-совместимый API (base_url меняется, код почти не трогается), самый дешёвый. Ключ `DEEPSEEK_API_KEY` уже используется в Kino-app.
  2. **Anthropic** (Claude): свой API-контракт, надо писать отдельный код-путь под `messages.create`. Ключ уже используется в WB-Bot и Kino-app `ai-chat`.
  3. **Gemini**: у Google есть OpenAI-совместимый endpoint (`https://generativelanguage.googleapis.com/v1beta/openai/`), дешёвый. Ключ используется в WB-Bot.
- [ ] **Fallback триггеры** — на какие ошибки переключаться:
  - Всегда: 5xx, timeout, network error
  - На 429: переключаться сразу или ретрай 1 раз на том же?
  - На 401/403: НЕ переключаться (это конфигурация, а не отказ провайдера) — но алертить в лог
- [ ] **Модель для fallback-провайдера** — какая? (для DeepSeek: `deepseek-chat`; для Claude: `claude-haiku-4-5-20251001`; для Gemini: `gemini-2.5-flash`).
- [ ] **Разные модели для `generate-lesson` (сложнее) и `lookup-word` (проще)** или одна?

### План (после утверждения решений)

- [ ] Добавить нужный секрет в Supabase Secrets проекта `ovhwxfdtkzwxfomdlgjv` (через Supabase Dashboard руками — не в коде).
- [ ] Отдельная ветка `claude/ai-provider-fallback`.
- [ ] Вынести функцию `callProvider(label, apiKey, baseUrl, model, useCompletionTokens)` в `supabase/functions/_shared/llm.ts` — единая для обеих функций, копирует паттерн из `Kino-app/supabase/functions/movie-recommendation/index.ts:160-200`. Учесть: у Technical-language сейчас `_shared` папки нет — создать.
- [ ] В `generate-lesson/index.ts` и `lookup-word/index.ts` заменить прямой `fetch('https://api.openai.com/...')` на `callWithFallback(primary, fallback, prompt, opts)`.
- [ ] Обработка результата (JSON parsing, валидация структуры) остаётся в вызывающей функции — fallback возвращает сырой текст ответа.
- [ ] Тело апстрима при ошибке — только в `console.error` (обрезать до 500 символов). Клиенту — фикстра. Правило [SEC-8], уже применено в edge functions Technical-language 2026-08-23.
- [ ] `SEC-7` fail-closed rate-limit сохранить — `checkRateLimit` вызывается до провайдера, не трогать.
- [ ] Записать в CLAUDE.md «Открытые баги» новый закрытый пункт `[REL-1] Один провайдер = точка отказа`.
- [ ] Записать в `MEMORY/lessons/lessons.md` (если формулировка урока эволюционировала) — иначе urok уже есть в `cross-repo-todo.md`, просто сослаться.
- [ ] Обновить `AI_OS/MEMORY/tasks/cross-repo-todo.md`: закрыть пункт «Один платный провайдер = одна точка отказа» для Technical-language, оставить для WB-Bot.
- [ ] PR не draft, ждать ручного мержа кнопкой. **НЕ мержить через API.**
- [ ] После мержа — задеплоить обе функции через Supabase MCP (`deploy_edge_function`), а не через GitHub Actions (T&S флаг активен).

### Критерии готовности

- [ ] При выключенном `OPENAI_API_KEY` (руками убрать из Supabase Secrets на 30 секунд) обе функции продолжают работать через fallback.
- [ ] При выключенном обоих ключах — фикстра 503, а не 500 с stacktrace.
- [ ] Rate limit fail-closed поведение не сломано.
- [ ] Никаких API-мержей. Никаких force-push.

### Не входит в скоуп (для другой сессии)

- UI-выбор провайдера пользователем в фронтенде.
- Метрики «сколько раз fallback сработал» — можно оставить только `console.error`, наблюдаемость через Supabase Logs.
- Sentry, DataDog и подобное.
- B7 (fail-closed rate-limit) — уже сделано в PR #53, задеплоено.
- B8 (`error.message` утечка) — уже сделано в PR #54, ждёт мержа + деплоя.

### Контекст этой сессии (кто это писал и зачем)

Это задание составлено в родительской AI_OS-сессии 2026-08-26 после того как:
1. Разобрали паттерны экосистемы (11 репо).
2. Прошли блок A — правки текстов CLAUDE.md.
3. Прошли B7 (fail-closed rate-limit) — задеплоено на прод.
4. Прошли B8 (error.message утечка) — Technical-language #54 ждёт мержа.
5. Дошли до B9 (LLM фолбэки) — это архитектурная работа, вынесена в отдельную Technical-language-сессию.

Владелица явно попросила: «Сделать выбор провайдера, задание для сессии где я веду техникал». Не начинать реализацию в этой родительской сессии.

### Первое действие в новой сессии

Не начинать код. Задать владелице 5 вопросов из «Решения, которые владелица должна утвердить» и дождаться ответа. Без утверждения — не менять код.
